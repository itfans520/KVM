### KVM 属于“硬件辅助虚拟化”，需要硬件宿主机支持虚拟化功能

## 检查硬件是否支持虚拟化功能

#intel CPU 架构检测命令
egrep 'xvm' /proc/cpuinfo

#AMD CPU 架构检测命令
egrep 'svm' /proc/cpuinfo

# 或者intel与AMD 一起检查
egrep '(svm | xvm)' /proc/cpuinfo


### 用户空间工具
（1）qemu
（2）libvirt(API)   #这个更好
